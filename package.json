{
  "name": "marketlens",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "test": "node scripts/market.test.mjs && node scripts/check_demo.mjs",
    "preview": "vite preview",
    "tauri": "tauri",
    "desktop": "tauri dev",
    "android": "tauri android dev"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "typescript": "^5.8.0",
    "vite": "^7.0.0"
  }
}
